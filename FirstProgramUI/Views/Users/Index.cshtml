@model IEnumerable<Model.Dtos.UserDto.UserGetDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<h1>Kullanıcılar</h1>
<a class="btn btn-primary mb-3" asp-action="Add">Ekle</a>

<table class="table table-bordered">
    <thead>
        <tr>
            <td>Kullanıcı Adı</td>
            <td>Şifre</td>
            <td>Mail</td>
            <td>Rol</td>
            <td>Şirket Adı</td>
            <td>Departman Adı</td>
            <td>İşlemler</td>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.Password</td>
                <td>@item.Email</td>
                <td>@item.AuthenticateName</td>
                <td>@item.CompanyName</td>
                <td>@item.DepartmentName</td>
                <td>
                    <a asp-action="Update" asp-route-id="@item.ID" class="badge alert-info">Düzenle</a>
                    <a asp-action="Delete" asp-route-id="@item.ID" class="badge alert-danger">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>